# Never commit credentials — these live in GitHub repo secrets instead.
.env
.env.*
!.env.example

node_modules/
queue.log
run.log
snapshots.jsonl

# Renders are fetched from the CDN at publish time, not stored in git.
*.mp4
*.jpg

# local helper that prints secrets to a console window
show_secrets.cmd

# local sync helpers, not part of the deploy
sync_all.cmd
sync_queue.cmd
